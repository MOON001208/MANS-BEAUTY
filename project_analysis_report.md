# 남성 화장품 시장 분석 프로젝트: 분석 과정 및 코드 설명서

이 문서는 프로젝트 진행 과정에서 작성된 주요 코드의 로직, 분석 방법, 그리고 의사결정 과정을 기록하여 향후 보고서 작성 시 기초 자료로 활용하기 위해 작성되었습니다.

---

## 1. 데이터 전처리 (Data Preprocessing)

### 1.1 전성분 데이터 정제 및 파싱 (`preprocess_ingredients.py`)

#### 1.1.1 배경 및 문제 정의
수집된 화장품 전성분 데이터(`product_ingredients.csv`)는 올리브영 웹페이지 구조상 다음과 같은 문제점이 있었습니다.
- **다중 상품 혼재**: '기획 세트' 상품의 경우, 메인 상품(예: 쿠션) 외에 증정품(예: 클렌징폼, 리필)의 전성분이 하나의 텍스트 필드에 `[상품명] 성분... [증정품] 성분...` 형태로 섞여 있습니다.
- **분석의 어려움**: 단순 텍스트 상태로는 특정 성분(예: 알레르기 유발 성분) 포함 여부를 판단하거나 통계적 분석을 수행하기 어렵습니다.

#### 1.1.2 해결 방안 (Logic)
이를 해결하기 위해 `preprocess_ingredients.py` 스크립트를 작성하여 다음과 같은 로직으로 데이터를 정제했습니다.

1.  **영역 파싱 (Parsing)**:
    - 정규표현식(`re`)을 사용하여 대괄호 `[...]`로 구분된 각 하위 상품 영역을 식별했습니다.
    - 예: `[본품] 정제수... [증정] 글리세린...` -> `{'본품': '정제수...', '증정': '글리세린...'}`

2.  **타겟 선정 알고리즘 (Heuristic Selection)**:
    - 어떤 성분 리스트가 분석 대상인 '메인 상품'인지 판단하기 위해 **텍스트 유사도 점수**를 계산했습니다.
    - 전체 상품명(예: "비레디 블루 쿠션 기획")과 파싱된 하위 상품명(예: "블루 쿠션") 간의 단어 교집합 개수를 점수화했습니다.
    - '증정', '클렌징' 등의 키워드가 포함된 경우 점수를 차감하여 증정품이 선택될 확률을 낮췄습니다.

3.  **데이터 구조화**:
    - 선택된 전성분 텍스트를 쉼표(`,`) 기준으로 분리하여 Python `list` 형태로 변환했습니다.
    - 이를 통해 추후 `ingredients_list`에 "티타늄디옥사이드"가 포함되어 있는지 쉽고 빠르게 조회할 수 있습니다.

#### 1.1.3 주요 코드 스니펫
```python
def parse_multi_ingredients(row):
    # 정규식으로 [상품명] 성분 텍스트 추출
    matches = re.findall(r'\[(.*?)\](.*?)(?=\[|$)', full_text)
    
    # 유사도 점수 계산 (메인 상품 찾기)
    for sub_name, ing_text in matches:
        score = len(main_tokens.intersection(sub_tokens))
        if '증정' in sub_name: score -= 5 # 증정품 페널티
        # ... (최적 매칭 선택)
```

#### 1.1.4 결과물
- **입력**: `data/product_ingredients.csv` (Raw Data)
- **출력**: `data/product_ingredients_clean.plk` (Pickle Format)
    - `ingredients_clean_str`: 정제된 단일 상품 전성분 텍스트
    - `ingredients_list`: 성분명 리스트 (예: `['정제수', '티타늄디옥사이드', ...]`)
    - `ingredient_count`: 성분 개수

---

## 2. (다음 단계 분석 내용 작성 예정)
