# 작업 보드

규칙은 [AGENTS.md](../AGENTS.md) 5장. 시작할 때 이름·시각을 적고, 끝나면 `[x]`로 바꾸고 결과를 한 줄 남긴다.
남이 점유한 항목은 건드리지 않는다. 항목마다 **건드리는 파일**을 적어 두었으니 겹치는지 먼저 본다.

형식: `- [ ] (에이전트, MM-DD HH:MM) 제목 — 파일: ...`

## 진행 중

- [ ] (claude, 09-19 01:12) **올리브영 남성 베이스 메이크업 전체 수집** — 파일: DB만 (코드 변경 없음)
  - `python -m scraper.crawler --products-only --write --max-catalog-pages 100 --delay 2.5 --max-seconds 5100`
  - 카탈로그 1,009개 탐색 완료. 상세 조회 진행 중. 중단돼도 `.pipeline-cache/catalog-details.json` 캐시로 이어서 진행
  - 끝나면 아래 "수집 직후" 항목들이 풀린다

## 수집 직후 (순서대로, 같은 사람이 이어서)

- [ ] **프로필 재생성** — 파일: DB만
  - `python pipeline/build_profiles.py --write`
  - 새로 채워진 `source_options`가 호수 라인업에 반영된다
- [ ] **호수 구분 현황 재측정** — 파일: 없음 (읽기만)
  - `python pipeline/shade_coverage.py`
  - 수집 전 기준선: 59개 중 A+B 26개(44.1%), 본래 단일 호수 8개 제외 시 51.0%
  - 톤로션 계열이 얼마나 올랐는지가 핵심 지표
- [ ] **스냅샷·평가 갱신** — 파일: `web/tests/fixtures/catalog-snapshot.json`
  - `cd web && npm run snapshot && npm run eval`
  - 수집 전 기준선: 호수없음 34.5%, 자체호수 60.7%, 고민적중 71.9%, 근거추적 100%
- [ ] **전체 검증 후 커밋** — 파일: 위 스냅샷
  - `python ops.py verify` 통과 확인 후 커밋·푸시

## 다음 (우선순위)

- [ ] **베이스 메이크업 소분류 목록 실측** — 파일: `scraper/crawler.py`
  - 중분류가 `베이스 메이크업`인 것만 확인했고 소분류는 `BB/CC` 하나만 직접 봤다
  - `details_cache`에 `lowerCategoryName`을 같이 기록하면 다음 수집에서 자동으로 모인다
  - 사용자가 직접 물어본 질문이라 답이 필요하다
- [ ] **샌드/앰버 밝기 분리** — 파일: `pipeline/profiles.py`, `tests/test_pipeline.py`
  - `BRIGHTNESS_WORDS`가 샌드와 앰버를 같은 단계로 묶어 `블랙몬스터 비비크림(샌드베이지/앰버베이지)`이 순서를 못 만든다
  - 통상 샌드 < 앰버. 분리하면 1개 상품 복구. 회귀 테스트 같이 추가할 것
- [ ] **톤로션 호수 커버리지 개선** — 파일: 미정
  - 쿠션 83~92% 대비 톤로션 33~42%. 입문자가 집는 카테고리라 체감 영향이 가장 크다
  - 수집이 끝난 뒤 얼마나 개선됐는지 먼저 측정하고 방향을 정한다
- [ ] **YouTube API 키 재발급 후 매핑 실행** — 파일: 없음 (사용자 작업 선행)
  - `스크래핑 코드/14_유튜브_댓글_크롤러.py` 28행에 키가 하드코딩돼 있다. 폐기·재발급 필요
  - 그 폴더는 gitignore에도 없어 `git add .` 한 번이면 올라간다
  - 이후 `python pipeline/shade_mapping.py --youtube`
  - 기대 수확량은 낮다(리뷰만으로 59개 중 1개 채택). 자막을 공식 API로 못 받는 것이 한계
- [ ] **추천 품질 검증셋 확장** — 파일: `web/tests/fixtures/personas.json`, `web/scripts/evaluate-recommendations.mjs`
  - 현재는 제약조건 충족률만 본다. 사람 평가 라벨이 생기면 NDCG/Recall로 확장 가능
  - 라벨 수집 방법부터 정해야 한다

## 보류 (사용자 판단 필요)

- [ ] **레거시 단계 정리** — `code/1_data_collection` ~ `code/6_web_recommendation`
  - 2026-01~02 이후 손대지 않았다. 보관할지 정리할지 결정 필요
- [ ] **`scraper/` 디버그 잔재** — `debug_*.py`, `debug_output*.txt`, `category_results*.txt`, `crawl_log.txt`, `test_ing*.py`
  - 커밋돼 있지만 현재 파이프라인이 쓰지 않는다

## 완료 (최근)

- [x] (claude, 09-18) 레거시 Supabase 키 폐기 검증 — 옛 키 401, 새 키 200 확인 (`833b49a`)
- [x] (claude, 09-18) 근거 리뷰 연결 + 추천 평가 하네스 (`4077893`)
- [x] (claude, 09-18) 호수 복구: 제품 내 밝기 순서 — 호수 보유 2개 → 26개 (`b993236`)
- [x] (claude, 09-18) 브랜드 호수 매핑 추출기 + 유튜브 수집기 (`328c270`, `3386cbf`)
- [x] (claude, 09-19) 수집 범위 확대: 컨실러 등 누락 해소, `--products-only` 추가 (`65223f5`)
- [x] (claude, 09-19) 운영 하네스 `ops.py` + 에이전트 계약 문서
