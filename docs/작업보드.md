# 작업 보드

규칙은 [AGENTS.md](../AGENTS.md) 5장. 시작할 때 이름·시각을 적고, 끝나면 `[x]`로 바꾸고 결과를 한 줄 남긴다.
남이 점유한 항목은 건드리지 않는다. 항목마다 **건드리는 파일**을 적어 두었으니 겹치는지 먼저 본다.

형식: `- [ ] (에이전트, MM-DD HH:MM) 제목 — 파일: ...`

## 진행 중

(없음)

## 다음에 확인할 것

- [ ] **Codex 로컬 수정 검수·커밋·배포** — 파일: 아래 완료 항목과 `docs/작업인계_2026-09-19_Codex.md` 참고
  - **사용자 배포 승인 완료(09-19).** 배포 요청 후 변경 파일만 `git add`했으나 `.git/index.lock: Permission denied`로 실패했다. 현재 배포는 미실행이다. 권한이 가능한 세션에서는 승인 재질문 없이 이어간다.
  - 09-19 Codex가 Claude의 수집·프로필·추천·운영 검사 경로를 직접 검수하고 수정했다.
  - 현재 환경에서 `.git/index.lock` 생성이 거부되어 점유 커밋부터 불가능했다. 로컬 변경을 유지한 상태다.
  - 다른 모델의 추가 검수 후 명시한 파일만 커밋·푸시한다. 사용자의 레거시 변경·개인 문서는 포함하지 않는다.
  - `origin/main`이 HEAD와 같으면 과거 커밋 검수가 되지 않는다. `--base <작업 전 커밋>`을 지정해야 한다.

- [ ] **원본 소분류·품절 호수 수정의 DB 적용** — 파일: DB, 이후 평가 스냅샷
  - 코드 수정 완료: 원본 소분류를 `profile_metadata.source_category`에 보존하고 비커버 유형을 퀴즈에서 제외한다. 파운데이션 `liquid`는 유지한다.
  - 로컬 캐시와 83개 스냅샷을 대조해 프라이머 5개·파우더 7개·쉐이딩 3개를 확인했다.
  - 상품만 수동 재수집 → 프로필 재생성(r6) → 공개 키 스냅샷 갱신 → verify/health/status 순서. README에 명령을 기록했다.
  - 이번 세션 DB 쓰기는 없었다. 라이브 조회는 네트워크 제한(WinError 10013/URLError)으로 실패했다.

## 다음 (우선순위)

- [ ] **립·아이 메이크업에 남성 상품이 있는지 제대로 확인** — 파일: `scraper/crawler.py`
  - 현재 `is_target`은 표준 중분류가 `베이스 메이크업`인 것만 통과시켜 립·아이를 **완전히 배제**한다
  - 근거는 각 카테고리 인기 상위 **표본 6개**뿐이다. 약한 근거로 범위를 잘라낸 상태
  - "남성 메이크업 전부"를 수집하려면 두 카테고리를 전수 스캔해 `menCategoryFlag` 상품이 정말 없는지 봐야 한다
  - 호수 매칭과는 무관하므로 우선순위는 낮지만, 범위 주장을 사실로 확정하려면 필요하다

- [ ] **리뷰 누적 수집** — 파일: DB만
  - 29개 상품에 5만 건 이상이 미수집 상태다. 한 번에 못 받는다
  - **확인된 규칙 (09-19, 실측)**: 벽은 깊이가 아니라 **세션당·정렬당 10페이지(100건)** 다.
    어젯밤 10번째 요청에서 `loginRequired`를 받은 커서 위치가, 새 세션에서는 열렸고
    거기서 정확히 10페이지(100건)를 더 받은 뒤 다시 벽에 걸렸다. 그래서 커서 재개로 누적이 된다
  - 이와 별개로, 여러 상품을 한 번에 연속 수집하면 서버가 긴 대기를 요구한다(`RateLimited`).
    09-19 02:47 실행은 3번째 상품에서 걸렸다. 한 번 실행에 1~2개 상품이 안전하다
  - 한 번 실행으로 상품당 최신순 기준 100건 + 다른 정렬 몇십 건이 늘어난다. 5만 건 전량은 현실적이지 않다.
    **리뷰 0건 상품 29개를 각각 순위에 오를 만큼(≥5건, 속성 점수에 ≥3건) 채우는 것**을 목표로 하는 게 맞다
  - 다른 정렬(평점순 등)은 최신순과 대부분 겹친다(평점 높은순은 10페이지에서 고유 12건). 정렬 선택 옵션이 있으면
    요청을 줄일 수 있지만 `scraper/crawler.py`에 Codex 미커밋 변경이 있어 지금은 손대지 않았다

- [ ] **톤로션 호수 커버리지 개선** — 파일: 미정
  - 전수 수집 후에도 톤로션 32개 중 A+B는 7개(21.9%)다. 쿠션/파운데이션은 43개 중 23개(53.5%)
  - 입문자가 집는 카테고리라 체감 영향이 가장 크다. 남은 24개는 대부분 F군(옵션 데이터 없음)
  - 다음 수단은 리뷰 수집이다. 신규 상품 24개는 리뷰가 0건이라 옵션명 근거가 아예 없다
- [ ] **YouTube API 키 재발급 후 매핑 실행** — 파일: 없음 (사용자 작업 선행)
  - `스크래핑 코드/14_유튜브_댓글_크롤러.py` 28행에 키가 하드코딩돼 있다. 폐기·재발급 필요
  - Codex가 해당 로컬 폴더를 `.gitignore`에 추가했다. 기존 하드코딩 파일과 키 폐기·재발급은 별도이며 완료하지 않았다.
  - 이후 `python pipeline/shade_mapping.py --youtube`
  - 기대 수확량은 낮다(리뷰만으로 59개 중 1개 채택). 자막을 공식 API로 못 받는 것이 한계
- [ ] **추천 품질 검증셋 확장** — 파일: `web/tests/fixtures/personas.json`, `web/scripts/evaluate-recommendations.mjs`
  - 현재는 제약조건 충족률만 본다. 사람 평가 라벨이 생기면 NDCG/Recall로 확장 가능
  - 라벨 수집 방법부터 정해야 한다

## 보류 (사용자 판단 필요)

- [ ] **레거시 단계 정리** — `code/1_data_collection` ~ `code/6_web_recommendation`
  - 2026-01~02 이후 손대지 않았다. 보관할지 정리할지 결정 필요
- [ ] **`scraper/` 디버그 잔재** — `debug_*.py`, `debug_output*.txt`, `category_results*.txt`, `crawl_log.txt`, `test_ing*.py`
  - 커밋돼 있지만 현재 파이프라인이 쓰지 않는다

## 완료 (최근)

- [x] (codex, 09-19 11:40) 사용자 기준 반영: 품절 호수 추천 복구 — 파일: `AGENTS.md`, 프로필·추천 로직, 호수 집계·평가·테스트, README·인계 문서
  - 품절 옵션을 라인업에 유지하고 판매 중 옵션 우선·모두 품절일 때 가장 가까운 호수 추천을 복구했다. 화면의 품절 표시 유지.
  - 품절 추천을 평가 위반에서 제거하고 품절 호수도 정보 보유 집계에 포함. 사용자 지시를 AGENTS.md에 기록했다.
  - `ops.py verify` 전 단계 통과(Python 66개·웹 20개 테스트, 평가·린트·빌드). 로컬 반영이며 DB·배포·커밋은 권한 제한으로 미실행.

- [x] (codex, 09-19 11:29) Claude 변경 검수 및 로컬 수정 — 파일: `ops.py`, `shared.py`, `scraper/crawler.py`, `pipeline/profiles.py`, `pipeline/shade_coverage.py`, `tests/`, `web/src/lib/`, `web/tests/recommendation.test.mjs`, `web/scripts/evaluate-recommendations.mjs`, `.gitignore`, README·인계 문서
  - 품절 호수 제외를 적용했다가 후속 사용자 확인에 따라 복구했다. 최종 기준은 품절 표시와 함께 추천 허용이다. 품절 기획과 판매 중 본품이 같은 호수인 경우 판매 중 옵션을 우선한다.
  - 원본 소분류 보존 및 추천 제외 연결. 기존 리뷰 프로필을 지우지 않으며 VERSION 유지, METADATA_REVISION만 6으로 변경.
  - 웹 검사 미실행을 실패 처리, 공개 리뷰 접근 확인·명시적 author 권한 거부 구분, 배포 연결 JS 키 검사, 호수 중복 집계 수정.
  - 빈 Git diff 교차검수 경고. 기존 스냅샷 12개 페르소나 평가: 필수 조건 위반 0건·근거 추적률 100%. 최신 DB·배포 검증으로 해석하지 않는다.
  - `ops.py verify` 통과: Python 66개·웹 20개 테스트, 추천 평가, 린트, 빌드 및 번들 키 검사.

- [x] (claude, 09-18) 레거시 Supabase 키 폐기 검증 — 옛 키 401, 새 키 200 확인 (`833b49a`)
- [x] (claude, 09-18) 근거 리뷰 연결 + 추천 평가 하네스 (`4077893`)
- [x] (claude, 09-18) 호수 복구: 제품 내 밝기 순서 — 호수 보유 2개 → 26개 (`b993236`)
- [x] (claude, 09-18) 브랜드 호수 매핑 추출기 + 유튜브 수집기 (`328c270`, `3386cbf`)
- [x] (claude, 09-19) 수집 범위 확대: 컨실러 등 누락 해소, `--products-only` 추가 (`65223f5`)
- [x] (claude, 09-19) 운영 하네스 `ops.py`(verify/health/status) + `AGENTS.md`·`CLAUDE.md`·작업보드 (`d160340`)
- [x] (claude, 09-19) 샌드/앰버 밝기 분리 — 일반어 `베이지`보다 구체어를 먼저 매칭 (`e98d985`)
- [x] (claude, 09-19) 스캔 상품의 원본 카테고리 기록 (`1e86443`)
- [x] (claude, 09-19) 프로젝트 현황 문서 (`98a3870`)
- [x] (claude, 09-19) Claude → Codex 자동 교차검수 `ops.py crosscheck` + `.mcp.json`
  - 실호출로 검증함. 현재는 Codex 사용량 한도 소진으로 대기
  - Antigravity는 헤드리스 모드가 없어 피호출자가 못 된다. Orca(1.4.205)도 지원하지 않음(Claude·Codex·Cursor만)
- [x] (claude, 09-19) 동시 읽기로 수집이 죽던 문제 수정 — `write_json` 재시도
- [x] (claude, 09-19) 컨실러를 전체 얼굴 추천에서 제외 — 페르소나 144건 중 5건이 컨실러였다
  - **사용자 확인 필요한 제품 판단.** 카탈로그·전체보기에는 그대로 남기고 퀴즈 추천에서만 뺐다
  - 지표는 소폭 하락(자체호수 60.7→59.5%, 부작용 0→1.0%). 빠진 컨실러가 호수 정보를 갖고 있었기 때문
- [x] (claude, 09-19) **베이스메이크업 1,009개 전수 수집 완료** — 상품 59 → **83개**
  - 남성 상품 78개 식별. 소분류 구성은 프로젝트 현황 문서 4-1장
  - `source_options` 5개 → 78개로 채워짐
- [x] (claude, 09-19) 품절 옵션을 호수 라인업에 남기고 표시 — 라인업 20 → 30개, 톤 구분 27.7% → 39.8%
  - 품절은 단종이 아닌데 호수 범위까지 숨기고 있었다. `두잉왓 블러링 필터 쿠션`은 4종인데 전부 품절이라 정보가 사라졌었다
  - `bestShadeOption`은 살 수 있는 옵션을 우선하고, 전부 품절일 때만 가장 가까운 것을 알린다
  - **최종 확인:** 사용자가 품절 호수 추천을 허용해 기존 동작을 유지하고 AGENTS.md의 금지 규칙을 갱신했다. 위 수치는 당시 측정 기준이다.
- [x] (claude, 09-19) `product_type` 재추론 버그 수정 — 26개 상품이 `cushion`으로 잘못 라벨돼 있었다
  - `build_profile`이 파생 카테고리(`쿠션/파운데이션`)로 타입을 다시 추론해 파운데이션·스틱·파우더가 전부 쿠션이 됐다
  - 교정 후 분포: cushion 17, tone_lotion 32, liquid 18, stick 8, concealer 8
- [x] (claude, 09-19) 리뷰 수집 1회분 — 3개 상품 154건. 분석 리뷰 25,664 → 25,818건
  - 리뷰 API 제약(정렬당 100건, 연속 요청 시 레이트 리밋)을 README에 기록했다
