# 구현 계획: 맞춤형 남성 화장품 추천 시스템

---

## 📋 구현 상태 (2026-01-26 업데이트)

| 단계 | 상태 | 설명 |
|------|------|------|
| 1. 환경 및 도구 설정 | ✅ 완료 | Python 환경, Gemini API 설정 완료 |
| 2. Gemini 텍스트 정규화 | ✅ 완료 | `reviews_normalized.plk` 생성됨 |
| 3. 리뷰 속성 추출 | ✅ 완료 | `extract_review_attributes.py` - 17,851개 리뷰 처리 |
| 4. 제품 성분 위험 매핑 | ✅ 완료 | `ingredient_risk_mapper.py` - 22개 제품 성분 분석 |
| 5. 맞춤형 추천 엔진 | ✅ 완료 | `content_based_recommender.py` - 가중 유사도 기반 |
| 6. 웹 프론트엔드 | ✅ 완료 | `streamlit_app.py` - 8가지 속성 입력 UI |

---

## 🎯 추천 시스템 방법론: 콘텐츠 기반 필터링

### 콘텐츠 기반 필터링(Content-Based Filtering)이란?
- **핵심 아이디어**: 사용자가 선호하는 아이템의 **특성(속성)**을 분석하여, 유사한 특성을 가진 다른 아이템을 추천
- **장점**: 다른 사용자 데이터가 필요 없음 (Cold Start 문제 완화)
- **본 프로젝트 적용**: 리뷰에서 추출한 제품 속성과 사용자 입력 선호도를 매칭

### 구현 흐름
```
[사용자 입력] → [사용자 프로필 생성] → [제품 프로필과 유사도 계산] → [상위 N개 추천]
     ↓                                      ↓
 피부타입, 고민,                      리뷰 기반 속성 +
 커버력 선호도 등                     성분 정보 벡터
```

---

## 1. 환경 및 도구 설정

### 로컬 Python 환경
- 가상 환경(`venv` 또는 `conda`) 생성 및 Python 3.9+ 설치
- 필요 라이브러리 준비:
  - 데이터 처리: `pandas`, `NumPy`
  - NLP: `KoNLPy`
  - API 클라이언트: `google-genai` (Gemini용), `streamlit`, `sklearn`

### OpenAI vs. Gemini
- GPT가 일반적이지만, **Google의 Gemini 2.5 Flash-Lite** 모델을 텍스트 작업에 사용
- Gemini Flash-Lite는 대용량, 저지연 작업에 최적화되어 있으며 매우 긴 컨텍스트(최대 1,048,576 토큰) 및 구조화된 JSON 출력 지원[^1][^2]
- 설치 및 구성:
  ```bash
  pip install google-genai
  ```
- `GEMINI_API_KEY` 환경 변수 설정

### 웹 프레임워크
- 프론트엔드에 **Streamlit** 사용 (무료 Python 기반 프레임워크)
- 수천 명의 AI 개발자가 Streamlit을 사용하여 생성형 AI 앱 구축 및 공유[^3]
- Streamlit Community Cloud를 통해 무료로 쉽게 배포 가능[^4]
- 대안: Gradio (Hugging Face Spaces에서 무료 호스팅 가능)[^5]

### 경량 데이터베이스/스토리지
- 정제된 리뷰 및 속성 점수를 CSV 또는 간단한 SQLite DB에 저장 계획
- Pandas는 CSV를 읽고 쓸 수 있으며 Streamlit과 원활하게 통합됨

---

## 2. 텍스트 정규화를 위한 Gemini API 통합

### 프롬프트 엔지니어링 정의
노이즈가 많은 한국어 리뷰를 명확하고 자연스러운 한국어로 다시 작성하도록 Gemini에 지시하는 시스템/사용자 프롬프트 작성:

```
System: "당신은 한국어 사용자 리뷰를 다시 작성하는 도우미입니다. 각 리뷰에는
        속어, 오타 또는 잘못된 문법이 포함될 수 있습니다. 원래 의미를 유지하면서
        정중하고 올바른 한국어로 다시 작성하세요."
User: "(노이즈가 있는 리뷰 텍스트)"
```

- 필요에 따라 스타일(격식/비격식 등) 조정
- 일관성을 위해 "구조화된 출력" 모드 사용 가능[^2]

### 배치 처리 스크립트
약 17,000개의 리뷰 텍스트를 순회하는 Python 스크립트 작성. 각 반복에서 GenAI SDK를 통해 Gemini 호출:

```python
from google import genai

client = genai.Client()  # GEMINI_API_KEY 사용
prompt = f"한국어 리뷰를 자연스럽고 정중하게 수정하세요:\n\n{raw_review_text}"
response = client.models.generate_content(
    model="gemini-2.5-flash-lite",
    contents=prompt
)
cleaned = response.text
```

- 정규화된 리뷰 텍스트 반환 후 CSV에 결과 추가
- 배치 호출은 속도 제한이 필요할 수 있음
- Gemini는 매우 큰 입력 크기를 지원[^6]하므로 JSON 형식으로 호출당 여러 리뷰 처리 가능

### 프롬프트 튜닝
- 문구 및 예제 실험. 예를 들어, 프롬프트에 1~2개의 모범 수정 예제 추가
- 출력 품질 모니터링
- 환각(hallucination)을 줄이기 위해 `temperature=0.0–0.5` 사용
- Gemini가 구조화된 출력을 지원하므로 JSON 요청 가능 (예: `response_format="json_schema"`)[^7]

---

## 3. 리뷰 속성 추출

### 속성 정의 (8가지 핵심 속성)
추출할 제품 측면 결정 - 콘텐츠 기반 필터링에 사용:

| 속성 | 설명 | 값 범위 |
|------|------|---------|
| 1. 피부 밝기 | 사용자의 피부톤에 맞는 호수 | 21호(밝은), 23호(중간), 25호(어두운) |
| 2. 피부고민 | 사용자의 피부 문제 | 여드름, 모공, 트러블, 홍조, 잡티, 없음 |
| 3. 피부타입 | 피부 유형 | 건성, 지성, 복합성, 민감성, 모든타입 |
| 4. 커버력 | 제품의 커버 정도 | 1-5점 (가벼운~풀커버) |
| 5. 지속력 | 제품의 지속 시간 | 1-5점 (짧은~하루종일) |
| 6. 가벼운 착용감 | 사용감의 가벼움 | 1-5점 (무거운~가벼운) |
| 7. 쿠션 vs 리퀴드 | 제형 선호도 | 쿠션, 리퀴드, 스틱 |
| 8. 성분정도 | 성분 안전성 | 자연유래, 저자극, 일반 |

### 추출 전략
각 정제된 리뷰에 대해 Gemini를 사용하여 이러한 속성을 점수화하거나 식별:

```
System: "이 화장품 리뷰를 분석하고 다음 속성에 대한 1-5점 점수를 JSON 객체로
        출력하세요: 유분 조절, 커버력, 지속력, 색상 매치. 언급된 주요 성분도
        기록하세요."
User: "(정제된 리뷰 텍스트)"
```

- `response_format="json_schema"` 또는 `json_object` 사용 시 Gemini가 구조화된 점수 반환[^2]
- 대안: 간단한 규칙 기반 접근법 - 키워드 검색 (예: "유분", "매트" → 유분 조절; "발림성", "밀착력" → 커버력)

### 스키마 및 저장
- 출력(파싱된 JSON 또는 DataFrame) 수집 후 구조화된 데이터셋(CSV 또는 데이터베이스)에 추가
- 각 행 = 각 속성에 대한 열이 있는 하나의 리뷰
- 모든 항목이 정렬되도록 보장 (예: 일관된 척도 사용)

---

## 4. 제품 성분 위험 매핑

### 성분 효과 컴파일
- 일반적인 화장품 성분과 피부 타입에 미치는 영향에 대한 소규모 조회 테이블 생성
- 예: 코메도제닉(comedogenic) 오일 및 자극제 기록
- 코코넛 오일은 모공을 막고 지성/여드름 피부를 악화시키는 것으로 알려져 있으므로 지성 피부는 피할 것[^8]
- 코코아 버터, 중알코올, 향료 등 포함

### 호환성 점수화
- 각 제품의 성분 목록에 대해 피부 타입별 호환성 점수 할당
- 예: 사용자가 지성 피부인 경우, 코메도제닉 오일에 대해 점수 차감
- 규칙 기반 조회로 가능

### 통합
- 리뷰 기반 속성과 함께 이러한 점수를 제품 프로필에 병합
- 각 제품 프로필에는 이제 **리뷰 기반 속성 + 성분 호환성 점수** 포함

---

## 5. 맞춤형 추천 엔진

### 사용자 입력 양식
사용자 선호도 수집:
- 피부 타입 (건성, 지성, 복합성)
- 피부 톤 (밝은, 중간, 어두운)
- 커버력 선호도 (얇은, 중간, 풀)
- 관심사 (매트 vs. 촉촉함) 등

### 매칭 로직
각 제품에 대해 제품 속성을 사용자 요구 사항과 비교하여 적합성 점수 계산:
- 예: 사용자가 지성 피부이고 제품의 "유분 조절" 점수가 높으면(좋음) 보너스 점수 부여
- "코코넛 오일"이 포함되어 있고 사용자가 지성 피부인 경우 페널티 부여
- 이를 전체 점수로 집계
- 프로토타입에는 간단한 가중 합계 또는 규칙 기반 점수화로 충분

### 순위 지정
- 점수별로 제품 정렬 후 상위 3개 선택

### Gemini를 통한 설명 생성
자연어 설명을 생성하기 위해 Gemini에 요약 생성 프롬프트:

```
User: "사용자는 (건성 피부, 매트 커버력 선호)입니다. 제품 X는 높은 지속력과
      좋은 유분 조절을 가지고 있으며, 성분 Y와 Z가 포함되어 있습니다.
      왜 이것이 좋은 매치인지 설명하세요."
Assistant: (Gemini는 "제품 X는 당신이 선호하는 매트 마감을 제공하고,
          피지를 효과적으로 조절하며, 피부 톤과 일치하기 때문에 추천됩니다..."
          같은 내용 작성)
```

- 관련 매치된 속성을 언급하도록 형식화된 프롬프트 사용
- Gemini의 생성 능력을 활용하여 사람이 읽을 수 있는 추론 작성

---

## 6. 웹 프론트엔드 (Streamlit)

### UI 요소
Streamlit 앱 구축:
- 입력 양식: `st.selectbox` (피부 타입), `st.slider` 또는 `selectbox` (커버력 선호도)
- 색상 선택기 또는 드롭다운 (톤)
- 특정 관심사를 위한 체크박스 (예: "유분 조절", "지속력")
- Streamlit의 간단한 위젯으로 빠른 프로토타이핑 가능

### 결과 표시
- 양식 제출 후 추천 로직 실행 및 상위 3개 제품 표시
- 각 제품에 대해 표시: 제품 이미지/이름, 주요 메타데이터 (속성, 성분 하이라이트), 생성된 설명 텍스트
- 레이아웃에 `st.write`, `st.markdown`, `st.expander` 사용

### UX 보장
- 데모 친화적으로 유지: API 호출 중 로딩 스피너, 명확한 레이블
- PoC이므로 앱에 무거운 최적화 불필요
- 사전 계산된 제품 프로필(속성, 성분)을 메모리 또는 소규모 DB에 저장하여 조회 속도 향상

### 로컬 테스트
- Streamlit 앱을 로컬에서 실행 (`streamlit run app.py`)
- 샘플 입력으로 엔드투엔드 테스트
- 레이아웃 및 텍스트 반복 개선

---

## 7. 선택사항: 배포

### Streamlit Community Cloud
- 무료 배포: `requirements.txt`와 함께 코드를 공개 GitHub 저장소에 푸시
- Streamlit Community Cloud를 통해 원클릭 배포[^4]
- 앱이 공개 URL 획득

### Gradio/HuggingFace Spaces (대안)
- 대신 Gradio를 인터페이스로 사용하는 경우 Hugging Face Spaces에서 호스팅
- HF Spaces는 데모 ML 앱에 대한 무료 호스팅 제공[^5]
- 프로젝트를 드래그 앤 드롭하거나 GitHub 저장소 연결 가능
- Spaces가 자동으로 빌드 및 배포

### 기타 플랫폼 (간략히)
- PythonAnywhere 또는 Railway와 같은 다른 무료 티어를 소규모 앱에 사용할 수 있음
- 그러나 Streamlit Cloud와 HF Spaces가 오픈 데모에 가장 간단함

---

## ✅ 구현 완료 (2026-01-26)

### 생성된 파일

#### 코드 파일 (`code/`)
| 파일명 | 설명 |
|--------|------|
| `extract_review_attributes.py` | 키워드 기반 리뷰 속성 추출 (커버력, 지속력, 착용감 등) |
| `ingredient_risk_mapper.py` | 성분 위험도 분석 및 피부타입별 호환성 점수 계산 |
| `build_product_profiles.py` | 제품별 8가지 속성 프로필 생성 |
| `content_based_recommender.py` | 콘텐츠 기반 필터링 추천 엔진 |
| `streamlit_app.py` | 웹 인터페이스 (Streamlit) |

#### 데이터 파일 (`data/`)
| 파일명 | 설명 |
|--------|------|
| `review_attributes.plk` | 리뷰별 추출된 속성 (17,851개) |
| `ingredient_risk_mapping.plk` | 제품별 성분 분석 결과 |
| `product_profiles.plk` | 24개 제품의 프로필 벡터 |

### 실행 방법

```bash
# 1. 속성 추출 (이미 완료됨)
python code/extract_review_attributes.py

# 2. 성분 분석 (이미 완료됨)
python code/ingredient_risk_mapper.py

# 3. 제품 프로필 생성 (이미 완료됨)
python code/build_product_profiles.py

# 4. 웹 앱 실행
streamlit run code/streamlit_app.py
```

### 추천 시스템 테스트 결과 예시
- 테스트 조건: 지성피부, 모공/여드름 고민, 커버력4, 지속력4, 쿠션 선호
- 1위 추천: **라끌랑 슈퍼쉴드 옴므쿠션** (매칭률 86.3%)

---

## 참고 자료 (Sources)

텍스트 작업에 Google의 Gemini 모델 및 API(GenAI SDK)[^1][^2][^9]와 AI 앱 배포에 대한 Streamlit의 공식 가이드[^3][^4]를 활용합니다. 스킨케어 성분 정보는 피부과 리소스[^8]에서 인용되었습니다. 이 계획은 AI 에이전트가 데이터 정제부터 작동하는 프로토타입까지 시스템을 엔드투엔드로 구현하도록 안내해야 합니다.

---

## 참조 링크

[^1]: [Gemini 2.5 Flash-Lite | Generative AI on Vertex AI | Google Cloud Documentation](https://docs.cloud.google.com/vertex-ai/generative-ai/docs/models/gemini/2-5-flash-lite)

[^2]: [Structured model outputs | OpenAI API](https://platform.openai.com/docs/guides/structured-outputs)

[^3]: [Streamlit • A faster way to build and share data apps](https://streamlit.io/generative-ai)

[^4]: [Deploying simple Streamlit apps - by Eric Matthes](https://mostlypython.substack.com/p/deploying-simple-streamlit-apps?utm_campaign=post&utm_medium=web)

[^5]: [Sharing Your App](https://www.gradio.app/guides/sharing-your-app)

[^6]: Gemini는 매우 큰 입력 크기 지원 (문서 내 언급)

[^7]: JSON 스키마 포맷 지원 (문서 내 언급)

[^8]: [Does Coconut Oil Clog Pores? Which Oils Don't?](https://www.medicinenet.com/does_coconut_oil_clog_pores_which_oils_dont/article.htm)

[^9]: [Gemini API quickstart | Google AI for Developers](https://ai.google.dev/gemini-api/docs/quickstart)